# Contributing

- Use feature branches and open PRs to `main`.
- Include validation evidence (tests/backtests/logs) in PR description.
- Keep changes scoped and reversible.
- Update docs when behavior/architecture changes.
